
{% extends 'base.html.twig' %}

{% block title %}login{% endblock %}
{% block montitre %} Connexion {% endblock %}

{% block inscription %}

    {% if error %}
        <div class="alert alert-danger">le login et mdp ne sont pas bon.</div>
    {% endif %}

    <form action="{{ path('login') }}" method="post">
        <div class="form-group">
            <label>Login :</label>
            <input type="text"  name="_username" class="form-control" value="{{ lastUserName }}">
        </div>
        <div class="form-group">
            <label>Password :</label>
            <input type="password"  name="_password" class="form-control">
        </div>
        <input type="submit" class="btn btn-primary"  value="s'identifier">
    </form>
<br>
    <div class="collapse navbar-collapse">
    <ul ><li><label>Vous n'avez pas un compte ?</label>
            <button class="book-btn"><a href="{{ path ('utilisateur_creation') }}">Inscription
                </a></button>
        </li></ul></div>


{% endblock %}