{% extends 'base.html.twig' %}

{% block title %}CategorieEmploi index{% endblock %}

{% block body %}
    <h1>CategorieEmploi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomEmploi</th>
                <th>DescriptionEmploi</th>
                <th>NbrOffres</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_emploi in categorie_emplois %}
            <tr>
                <td>{{ categorie_emploi.id }}</td>
                <td>{{ categorie_emploi.nomEmploi }}</td>
                <td>{{ categorie_emploi.descriptionEmploi }}</td>
                <td>{{ categorie_emploi.nbrOffres }}</td>
                <td>
                    <a href="{{ path('categorie_emploi_show', {'id': categorie_emploi.id}) }}">show</a>
                    <a href="{{ path('categorie_emploi_edit', {'id': categorie_emploi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_emploi_new') }}">Create new</a>
{% endblock %}
