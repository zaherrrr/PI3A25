{% extends 'base2.html.twig' %}

{% block title %}PostulerEmploi index{% endblock %}

{% block body %}
    <h1>PostulerEmploi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DatePostulation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for postuler_emploi in postuler_emplois %}
            <tr>
                <td>{{ postuler_emploi.id }}</td>
                <td>{{ postuler_emploi.datePostulation ? postuler_emploi.datePostulation|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('postuler_emploi_show', {'id': postuler_emploi.id}) }}">show</a>
                    <a href="{{ path('postuler_emploi_edit', {'id': postuler_emploi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('postuler_emploi_new') }}">Create new</a>
{% endblock %}
