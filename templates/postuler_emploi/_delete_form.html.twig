<form method="post" action="{{ path('postuler_emploi_delete', {'id': postuler_emploi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postuler_emploi.id) }}">
    <button class="btn">Delete</button>
</form>
