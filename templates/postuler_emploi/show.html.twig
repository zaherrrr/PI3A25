{% extends 'base2.html.twig' %}

{% block title %}PostulerEmploi{% endblock %}

{% block body %}
    <h1>PostulerEmploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postuler_emploi.id }}</td>
            </tr>
            <tr>
                <th>DatePostulation</th>
                <td>{{ postuler_emploi.datePostulation ? postuler_emploi.datePostulation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('postuler_emploi_index') }}">back to list</a>

    <a href="{{ path('postuler_emploi_edit', {'id': postuler_emploi.id}) }}">edit</a>

    {{ include('postuler_emploi/_delete_form.html.twig') }}
{% endblock %}
