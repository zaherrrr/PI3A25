{% extends 'base2.html.twig' %}

{% block title %}CategorieFormation index{% endblock %}

{% block body %}
    <h1>CategorieFormation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_cat_frt</th>
                <th>Description_cat_frt</th>
                <th>Nbr_cat_frt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_formation in categorie_formations %}
            <tr>
                <td>{{ categorie_formation.id }}</td>
                <td>{{ categorie_formation.nomCatFrt }}</td>
                <td>{{ categorie_formation.descriptionCatFrt }}</td>
                <td>{{ categorie_formation.nbrCatFrt }}</td>
                <td>
                    <a href="{{ path('categorie_formation_show', {'id': categorie_formation.id}) }}">show</a>
                    <a href="{{ path('categorie_formation_edit', {'id': categorie_formation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_formation_new') }}">Create new</a>
{% endblock %}
