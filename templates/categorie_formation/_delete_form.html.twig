<form method="post" action="{{ path('categorie_formation_delete', {'id': categorie_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_formation.id) }}">
    <button class="btn">Delete</button>
</form>
